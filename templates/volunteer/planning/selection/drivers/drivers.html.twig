<div class="modal-header">
        <div class='row col-12 gap-3'>
            <h5 class="modal-title col-4" id="removalsModalLabel">Définir le conducteur</h5>
            <div id='input-search' class="col-4">
                {% include 'volunteer/planning/component/search.html.twig' with {'type':'driver'} %}
            </div> 
            <button type="button" class="btn-close col-3 mt-1" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
</div>
                {% include 'volunteer/planning/component/volunteerGrid.html.twig' %}
{#<div class="modal-body" id="modal-body">
	<div class='row'>
		{% for driver in drivers %}
			<div class='col-3 ajax-link-action' action="{{path('app_volunteer_planning_select_driver',{'driverId':driver.id,'pLineId':pLine.id})}}" data-target-spinner='select-driver-{{driver.id}}' data-spinner-type='2' id='select-driver-{{driver.id}}'>
				{% include 'volunteer/planning/selection/drivers/cell.html.twig' with {'active':(driver == pLine.driver)?true:false} %}
			</div>
		{% endfor %}
	</div>
</div>#}