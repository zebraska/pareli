<div class='col'>
	<span action="{{path(route)}}" class='ajax-link-action' data-target-spinner='body-interface' data-spinner-type='1'>
		<div class='text-center menu-title'>{{title}}</div>
		<div class='text-center'>
			<div>
				<div class='btn {{ controlerName ? 'btn-success' : 'btn-primary'}} rounded-circle' style='height:10vh;width:10vh;'>
					<i class="{{icon}}" style='font-size:5vh;padding:1vh'></i>
				</div>
			</div>
		</div>
	</span>
</div>
